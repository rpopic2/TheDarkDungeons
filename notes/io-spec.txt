pr(object 출력) 콘솔에 쓰기. object가 Array면 format해서 보여줌 -b 맨 아랫줄에 쓰기
rk([string 출력]) 키 읽어들임. 입력하는 글자는 보이지 않음.
del([int 줄 수]) 지정한 줄 수 만큼 지움.

int seln(Array 선택지) 선택지를 띄워주고 숫자(n)를 누르면 몇 번째 선택지를 골랐는지 반환.
int seli() 인벤토리를 띄워주고 w~y를 누르면 몇 번째를 골랐는지 반환.

int chkn(Char 문자, int 최대) 해당 문자를 숫자로 반환. 최대보다 높거나 숫자가 아니면 -1반환.
int chki(Char 문자) 해당 문자가 w~y에 해당하는지 확인하고, 몇 번째인지 숫자로 반환. 해당하지 않으면 -1반환